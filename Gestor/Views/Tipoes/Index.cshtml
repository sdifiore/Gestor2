@model IEnumerable<Gestor.Models.Tipo>

@{
    ViewBag.Title = "Índice";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Índice</h2>

<p>
    @Html.ActionLink("Criar novo tipo", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.Apelido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { id = item.TipoId }) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.TipoId }) |
            @Html.ActionLink("Elminar", "Delete", new { id = item.TipoId })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apelido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { id = item.TipoId }) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.TipoId }) |
            @Html.ActionLink("Elminar", "Delete", new { id = item.TipoId })
        </td>
    </tr>
}

</table>
